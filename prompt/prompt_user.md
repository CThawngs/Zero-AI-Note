# User Contract — Zero AI Note

## Binding preferences

- keep going until done. stop if work needs credentials.
- prefer concise commits over long messages.

## Required behavior

- Detect `prompt_embeddings/continuity.md` and `prompt/prompt_user.md`.
- Treat them as the standalone contract for future runs.
- Do not ask the user to repeat previous decisions.
- Do not summarize these files on every turn.