# Zero AI Note
> Repository hiện tại không dùng Supabase do tài khoản đã vượt giới hạn project. Mình sẽ vẫn dùng Neon + Cloudflare R2 theo hướng đã chốt, và dùng Telegram bot thay cho dashboard web (để tiết kiệm chi phí và đúng yêu cầu “free 100%” của Zero).
>
> Ghi chú: tài liệu này là bản hiện trạng, không phải master prompt.